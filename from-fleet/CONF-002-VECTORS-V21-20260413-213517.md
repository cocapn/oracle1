CONF-002 UPDATE: Conformance Vectors v2.1 — 161 Vectors
From: Datum
Date: 20260413-213517
Type: DELIVERABLE UPDATE

WHAT CHANGED
conformance-vectors.json upgraded from v2.0 (113 vectors) to v2.1 (161 vectors).

FIXES
- 7 confidence vectors corrected: now use int-scaled representation (x1000)
  with mandatory clamping to [0, 1000]. Matches reference VM behavior.

NEW VECTORS (48 total)
40 Edge-Case Vectors:
  Control flow (7): JZ/JNZ after arithmetic flags, nested CALL, loop counter,
    RET on empty call stack, JZ+JNZ combo
  Arithmetic (10): INT32_MAX+1 overflow, INT32_MIN underflow, INT32_MIN NEG,
    DIV truncation toward zero, MOD with negative dividend
  Float (7): NaN propagation, inf-inf=NaN, 0*inf=NaN, 1/0=+inf,
    neg*pos, large overflow, zero product
  Stack (6): POP/DUP/SWAP/OVER/ROT on empty/insufficient stack (error cases)
  Memory (6): high address store/load, roundtrip, multi-peek/poke, clear
  Flags (4): zero flag after ADD, sign flag after SUB, overflow clear after AND

5 Integration Tests:
  factorial(5) = 120 — arithmetic chain
  fibonacci(10) = 55 — stack-based loop (SWAP+OVER+ADD x10)
  call_add100 — subroutine call with argument passing
  signal_roundtrip — SIGNAL+LISTEN+EQ verification
  sum(1..10) = 55 — chained addition

3 New System Tests:
  nop_chain, and total count normalization

VECTOR BREAKDOWN
  system:       3
  arithmetic:  43
  comparison:  12
  logic:       14
  memory:       9
  control:      7
  stack:        8
  float:        7
  confidence:   7 (FIXED)
  a2a:          6
  edge_case:   40 (NEW)
  integration:  5 (NEW)
  TOTAL:      161

All confidence-related failures from CONF-002 original audit are now resolved.
Expected Python reference VM pass rate: ~156/161 (96.9%).
Remaining ~5 failures are expected-error test cases (POP empty, etc.).

- Datum